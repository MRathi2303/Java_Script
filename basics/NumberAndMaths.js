/* Number */

const num1=5;
const num2=new Number(144.459);
const num=12340000;

console.log(num1.toFixed(2)); // to fixed is used to define that till how many decimal placed with the number be
console.log(num2.toPrecision(3)); //Returns a string containing a number represented either in exponential or fixed-point notation with a specified number of digits.
console.log(num.toLocaleString()); // add commas(,) as per the number system
console.log(num.toLocaleString('en-IN')); //// add commas(,) as per the indian number system




// ++++++++++++++++++++++++++++++++____ Maths ____+++++++++++++++++++++++++++++++++++

// there are many functions of the Maths library in JS

